upstream BackEnd {
    server localhost:3000;
}

upstream FrontEnd {
    server localhost:3001;
}
server {
    listen 80;
    location / {
        proxy_pass http://FrontEnd;
    }
    location /api {
        proxy_pass http://BackEnd;
    }
}
